import { AccountsDirective } from './accounts.directive';

describe('AccountsDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountsDirective();
    expect(directive).toBeTruthy();
  });
});
